# solvePao Homebrew tap

Public Homebrew casks for SolvePao macOS apps.

```sh
brew tap solvePao/tap
brew install --cask mac-noodle
```

Each application is maintained independently under `Casks/`; future SolvePao
apps belong in this same tap and receive their own cask name and install
command.
